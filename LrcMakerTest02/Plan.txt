欢迎试用本软件！

当前版本：beta 2.2.0

v2.2.1
读取没有时间轴的歌词时，会保留空白行；
修复插入行时音乐暂停，但是按钮没有变为“播放”的BUG；
添加快捷键：更新（F5）

v2.2.0
添加“时间整体偏移”功能；
由于上次更新的时候过于智商下线，导致软件没有图标，现已纠正；
关闭时会提示是否保存（如果否，而且有临时文件，则会删除）；
而且开启软件时，如果发现有临时文件，会自动读取；

beta v2.1.0
将“插入行”修改为在当前行下方插入；
添加更为详细的微调按钮（+1s 并没有别的特殊含义）；
可以在下方的当前行歌词时间中使用滚轮键调整时间（每次变化 50 毫秒）；
修复暂停状态下按下“停止”后，暂停按钮没有恢复为“播放”；
更换图标；
在没有播放的情况下，即使勾选“播放时测试”，也不会实时锁定当前时间所对应歌词；
将默认的时间偏差改为200毫秒；

beta v2.0.5

改进 UI 效果；
现在可以识别单行的多个时间框了；
改进部分算法；
将“播放”与“暂停”合并为一个按钮；

beta v2.0.0

使用 WPF 重建 UI，并重现 v1.0.0 版全部功能；
采用更健全的方法记录歌词时间轴；

======== 未来计划 ========

这个版本仍然不好用，我承认……

由于 WPF 中 ListBox 的功能限制，“播放时测试”的跳转功能并不十分好用，但是也能凑活。
导入有时间轴的歌词时，如果歌词开头带有信息内容（同样使用“[]”括住的文本），将会报错。
在无歌词的时候，写入文本是暂停在一句话开头的，这时这句话的内容用户可能并不知道，需要听完这一句，然后倒退回该句开头，然后插入。
使用 .Net 的 MediaElement，无法播放无损音频。

以上缺点，将会在正式版中进行弥补。然而正式版遥遥无期（手动哭笑不得），请大家见谅。
如果您认为这款软件很棒，而且迫切期待作者推出正式版，或者您有意见和建议，欢迎您联系我的邮箱：byjr_k@163.com